name: Release (tags)

on:
  push:
    tags:
      - 'v*'
   workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Am primit tag-ul: ${{ github.ref_name }}"
